# PyAgentKit 依赖
# LLM 集成（智谱 GLM）
zhipuai>=2.1.0

# HTTP 请求（WebSearchTool / MCP 工具）
requests>=2.28.0

# .env 文件自动加载
python-dotenv>=1.0.0

# YAML 配置驱动
pyyaml>=6.0

# 向量记忆（本地 TF-IDF embedder + cosine 相似度）
numpy>=1.21.0
scikit-learn>=1.0.0

# 测试（开发依赖）
pytest>=7.0.0
